# 💾 Руководство по кешированию и синхронизации

## Обзор

Система кеширования позволяет приложению работать оффлайн, сохраняя данные локально и синхронизируя их с сервером при восстановлении подключения к интернету.

## Архитектура

### 1. CacheService (`lib/core/services/cache_service.dart`)
- Сохраняет товары локально в SharedPreferences
- Загружает товары из кеша
- Обновляет отдельные товары в кеше
- Управляет временными метками кеша

### 2. SyncService (`lib/core/services/sync_service.dart`)
- Создает очередь операций синхронизации
- Синхронизирует изменения с сервером
- Обрабатывает ошибки и повторные попытки
- Сохраняет только неудавшиеся операции для повторной синхронизации

### 3. ConnectivityService (`lib/core/services/connectivity_service.dart`)
- Проверяет наличие подключения к интернету
- Отслеживает изменения подключения в реальном времени
- Уведомляет о восстановлении/потере подключения

## Как это работает

### Первый запуск
1. Приложение пытается загрузить товары с сервера
2. При успешной загрузке товары сохраняются в кеш
3. Товары отображаются в приложении

### Работа с интернетом
1. Все операции выполняются через API
2. Кеш обновляется после успешных операций
3. Изменения сразу видны в приложении

### Работа без интернета
1. Товары загружаются из кеша
2. Все изменения добавляются в очередь синхронизации
3. Кеш обновляется локально
4. Пользователь может продолжать работу

### Восстановление интернета
1. ConnectivityService обнаруживает восстановление подключения
2. SyncService автоматически синхронизирует очередь изменений
3. Кеш обновляется с актуальными данными с сервера
4. Неудавшиеся операции остаются в очереди для повторной попытки

## Использование

### Загрузка товаров с кешированием
```dart
// Сначала пытаемся загрузить с сервера
// Если нет интернета - загружаем из кеша
final hasInternet = await ConnectivityService().hasConnection();
if (hasInternet) {
  // Загрузка с сервера + сохранение в кеш
} else {
  // Загрузка из кеша
}
```

### Обновление товара
```dart
// Обновляем в кеше сразу
// Добавляем в очередь синхронизации
// Синхронизируем при наличии интернета
```

### Сканирование товара
```dart
// Ищем товар в кеше
// Если найден - показываем актуальные данные
// Обновляем товар в кеше после изменений
```

## Файлы

- `lib/core/services/cache_service.dart` - Сервис кеширования
- `lib/core/services/sync_service.dart` - Сервис синхронизации
- `lib/core/services/connectivity_service.dart` - Сервис проверки подключения

## Следующие шаги

1. Интеграция кеширования в ProductProvider
2. Автоматическая синхронизация при восстановлении интернета
3. Уведомления пользователя о статусе синхронизации
4. Обработка конфликтов данных
