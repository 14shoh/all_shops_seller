# 🔥 Hot Reload - Горячая перезагрузка

## Что такое Hot Reload?

Hot Reload позволяет видеть изменения в коде **мгновенно** без полного перезапуска приложения. Это значительно ускоряет разработку!

## Как использовать Hot Reload

### Способ 1: Автоматический (рекомендуется)

1. **Запустите приложение в режиме отладки:**
   ```bash
   cd seller
   flutter run
   ```

2. **Сохраните файл (Ctrl+S)** - изменения применятся автоматически!

### Способ 2: Ручной Hot Reload

После запуска `flutter run`, в терминале доступны команды:

- **`r`** - Hot Reload (быстрая перезагрузка)
- **`R`** - Hot Restart (полная перезагрузка с сохранением состояния)
- **`q`** - Выход

### Способ 3: Через VS Code

1. Нажмите **F5** или выберите "Run > Start Debugging"
2. Приложение запустится в режиме отладки
3. После изменения кода нажмите **Ctrl+S** (сохранить)
4. Изменения применятся автоматически!

## Что можно обновлять через Hot Reload?

✅ **Работает:**
- Изменения в UI (виджеты, стили, цвета)
- Изменения в логике виджетов
- Изменения в методах
- Изменения в переменных

❌ **Требует Hot Restart (R):**
- Изменения в `main()` функции
- Изменения в статических переменных
- Изменения в глобальных переменных
- Изменения в enum
- Изменения в инициализации классов

## Быстрые клавиши

- **Ctrl+S** - Сохранить и применить изменения (автоматический Hot Reload)
- **Ctrl+Shift+F5** - Hot Restart
- **Ctrl+F5** - Полный перезапуск

## Настройки VS Code

Файл `.vscode/settings.json` уже настроен для автоматического Hot Reload при сохранении файла.

## Решение проблем

### Hot Reload не работает?

1. Убедитесь, что приложение запущено через `flutter run` (не через установленный APK)
2. Проверьте, что вы в режиме Debug (не Release)
3. Попробуйте Hot Restart (R) вместо Hot Reload (r)
4. Перезапустите приложение полностью

### Изменения не видны?

1. Некоторые изменения требуют Hot Restart (R) вместо Hot Reload (r)
2. Проверьте консоль на наличие ошибок
3. Убедитесь, что файл сохранен (Ctrl+S)

## Пример использования

```dart
// 1. Запустите: flutter run
// 2. Измените цвет:
Container(
  color: Colors.blue, // Было: Colors.red
  child: Text('Hello'),
)
// 3. Сохраните (Ctrl+S) - изменения применятся сразу!
```
